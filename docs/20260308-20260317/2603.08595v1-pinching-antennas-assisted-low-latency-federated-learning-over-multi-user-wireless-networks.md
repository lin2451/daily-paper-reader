---
title: Pinching Antennas-Assisted Low-Latency Federated Learning Over Multi-User Wireless Networks
title_zh: 多用户无线网络中夹持天线辅助的低延迟联邦学习
authors: "Saba Asaad, Hina Tabassum, Ping Wang"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08595v1"
tags: ["query:fl-ra"]
score: 9.0
evidence: 联合最小化无线联邦学习中的端到端轮次延迟和最优性间隙
tldr: 针对无线联邦学习中通信链路受衰落和遮挡影响导致的延迟问题，本文提出FedPASS框架，利用可移动的收缩天线系统（PASS）动态调整辐射点位置以优化信道质量。通过联合优化设备调度、功率控制、计算频率及天线位置，并采用双层迭代算法求解，该方案在保证模型精度的同时显著降低了训练总延迟。
selection_source: fresh_fetch
motivation: 传统无线联邦学习受限于信道衰落和遮挡导致的通信瓶颈，亟需通过物理层天线位置的动态调整来提升链路质量和传输效率。
method: 提出FedPASS框架并构建多目标优化模型，采用块坐标下降与高斯-赛德尔迭代算法协同优化资源分配、调度策略及天线位置。
result: 在MNIST和CIFAR-10数据集上的实验证明，FedPASS在达到理想精度的同时，相比传统无线联邦学习方案大幅缩减了总训练延迟。
conclusion: 引入收缩天线系统能有效改善无线环境下的通信可靠性，是实现低延迟、高性能分布式机器学习的创新且有效的途径。
---

## 摘要
无线网络上的联邦学习（FL）从根本上受到不可靠通信链路的限制，特别是当上行信道遭受遮挡、衰落或弱视距（LoS）条件时。夹持天线系统（PASS）提供了一种新的物理层能力，可以沿介质波导动态重新定位辐射点，从而实现可控的视距连接并显著提高信道质量。本文开发了 FedPASS，这是一个由 PASS 辅助的低延迟无线联邦学习的新型框架。我们制定了一个多目标优化问题，共同最小化端到端轮次延迟和联邦学习最优性间隙的上界。由此产生的公式是一个混合整数非线性规划问题，受限于调度、发射功率、本地 CPU 频率和夹持天线（PA）放置的实际约束。为了应对由此产生的计算挑战，我们开发了一种两层迭代算法：外层循环通过块坐标下降更新调度、通信时间分配和功率控制；内层循环在间距约束下，使用基于 Gauss-Seidel 的坐标更新和网格搜索来优化 PA 位置。在 MNIST 和 CIFAR-10 上的数值结果表明，FedPASS 实现了与理想化联邦学习基准相当的准确度，同时与传统的无线联邦学习相比，大幅降低了总训练延迟。

## Abstract
Federated learning (FL) over wireless networks is fundamentally constrained by unreliable communication links, particularly when uplink channels suffer from blockage, fading, or weak line-of-sight (LoS) conditions. Pinching-antenna systems (PASSs) offer a new physical-layer capability to dynamically reposition radiating points along a dielectric waveguide, enabling controllable LoS connectivity and significantly improved channel quality. This paper develops FedPASS, a novel framework for low-latency wireless FL assisted by PASS. We formulate a multi-objective optimization problem that jointly minimizes the end-to-end round latency and an upper bound on the FL optimality gap. The resulting formulation is a mixed-integer nonlinear program subject to practical constraints on scheduling, transmit power, local CPU frequency, and PA placement. To address the resulting computational challenges, we develop a two-tier iterative algorithm: an outer loop that updates scheduling, communication time allocation, and power control via block coordinate descent, and an inner loop that optimizes PA locations using a Gauss-Seidel-based coordinate update with grid search under spacing constraints. Numerical results on MNIST and CIFAR-10 demonstrate that FedPASS achieves accuracy comparable to idealized FL baselines while drastically reducing the total training latency compared to conventional wireless FL.

---

## 论文详细总结（自动生成）

这是一份关于论文《Pinching Antennas-Assisted Low-Latency Federated Learning Over Multi-User Wireless Networks》的结构化深入总结：

### 1. 核心问题与整体含义
*   **背景与动机**：无线联邦学习（FL）的性能高度依赖于上行通信链路的质量。在实际场景中，由于遮挡、衰落或弱视距（LoS）条件，部分设备（掉队者）会严重拖慢整体训练进度。
*   **核心问题**：现有的可重构技术（如RIS、流体天线）调整范围极小（波长级），难以补偿大尺度的路径损耗。
*   **研究目标**：引入**夹持天线系统（PASS）**，利用其长介质波导（可达数十米）动态重新定位辐射点（PA），为移动设备提供可控的强视距连接，从而在物理层根本性地解决无线FL的延迟与收敛瓶颈。

### 2. 方法论
*   **核心思想**：提出 **FedPASS 框架**，通过联合优化天线位置、设备调度、发射功率和计算频率，实现训练延迟与学习精度（最优性间隙）之间的最优权衡。
*   **关键技术细节**：
    *   **多目标优化建模**：构建了一个混合整数非线性规划（MINLP）问题，目标是最小化“端到端轮次延迟”和“FL最优性间隙上界”的加权和。
    *   **最优性间隙推导**：在平滑性和PL条件下，推导了包含聚合误差（由部分参与引起）和局部漂移误差的收敛上界。
*   **算法流程（两层迭代）**：
    *   **外层循环（BCD）**：利用块坐标下降法，将原问题分解为延迟最小化、设备调度（线性规划）和功率控制（凸优化）三个子问题交替求解。
    *   **内层循环（Gauss-Seidel）**：针对非凸的天线位置优化，采用高斯-赛德尔坐标更新。通过在波导上进行一维网格搜索，在满足天线最小间距约束的前提下，逐个优化PA位置以最大化信道增益。

### 3. 实验设计
*   **数据集**：MNIST（手写数字识别）和 CIFAR-10（彩色图像分类）。
*   **场景设置**：30m × 20m 的矩形区域，12个随机分布的用户，采用 Dirichlet 分布（$\alpha=0.35$）模拟非独立同分布（Non-IID）数据。
*   **对比基准（Benchmarks）**：
    *   **Perfect FL**：理想信道，所有设备每轮均参与，作为性能上限。
    *   **Conventional FL**：采用固定位置天线的传统无线基站。
    *   **PASS-Uniform**：使用PASS系统，但天线在波导上均匀固定分布，不进行动态位置优化。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件资源（如 GPU 型号、数量）或实际物理训练时长。
*   **分析**：由于该研究侧重于通信物理层与优化算法的数值仿真，实验结果主要以“通信轮数”和理论计算的“总延迟（秒）”为度量标准，而非实际挂钟时间。

### 5. 实验数量与充分性
*   **实验规模**：做了多组实验，包括不同数据集（MNIST/CIFAR-10）的准确率对比、Pareto前沿分析、以及针对用户数（K）、发射功率（P）、天线数（N）和数据量（Db）的参数敏感性分析。
*   **充分性与客观性**：实验设计较为充分，通过 Non-IID 数据设置增加了任务难度，客观展示了 PASS 在改善弱信道设备参与度方面的优势。对比基准涵盖了理想状态、传统状态和无优化状态，论证逻辑严密。

### 6. 主要结论与发现
*   **延迟显著降低**：FedPASS 在达到相同目标精度时，比传统固定天线方案快约 **6.4 倍**。
*   **精度优势**：由于 PASS 能通过优化位置增强弱信道用户的信号，使得更多设备能有效参与聚合，从而显著降低了 Non-IID 场景下的聚合误差，精度直逼理想 FL。
*   **功率与规模敏感性**：在低功率预算或大规模用户场景下，PASS 的位置优化增益更加突出，能有效缓解“掉队者”问题。

### 7. 优点
*   **物理层与学习层的深度耦合**：不同于仅关注通信速率的研究，本文直接将天线位置优化与 FL 的收敛理论（最优性间隙）挂钩。
*   **PASS 技术的创新应用**：利用 PASS 大范围移动辐射点的特性，解决了传统可重构天线无法应对大尺度衰落的局限性。
*   **算法实用性**：提出的两层迭代算法复杂度可控（主要受用户数立方级影响），具有较强的工程实现潜力。

### 8. 不足与局限
*   **环境模型简化**：实验主要基于视距（LoS）路径损耗模型，未深入探讨在复杂多径、非视距（NLoS）或高动态干扰环境下的表现。
*   **部署成本与限制**：PASS 依赖于物理介质波导的铺设，这限制了其在开阔户外环境的应用，更适用于工厂、仓库或室内办公环境。
*   **异步性缺失**：目前仅讨论了同步 FL 协议，未探讨 PASS 如何与异步 FL 结合以进一步处理极端的掉队者情况。

（完）
