---
title: Edge Learning via Federated Split Decision Transformers for Metaverse Resource Allocation
title_zh: 基于联邦拆分决策 Transformer 的元宇宙资源分配边缘学习
authors: "Fatih Temiz, Shavbo Salehi, Melike Erol-Kantarci"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16174v1"
tags: ["query:fl-ra"]
score: 9.0
evidence: 通过联邦学习为虚拟现实用户进行智能资源分配
tldr: 针对元宇宙服务中移动边缘计算（MEC）资源分配的挑战，本文提出了一种名为联邦拆分决策Transformer（FSDT）的离线强化学习框架。该框架通过将Transformer模型在MEC服务器和云端之间进行拆分，既实现了本地自适应性，又通过云端共享层促进了协作训练。实验表明，FSDT在异构环境下显著提升了用户体验质量（QoE），并大幅减轻了边缘端的计算负担。
selection_source: fresh_fetch
motivation: 传统的联邦学习在异构边缘计算环境中面临模型参数传输开销大以及全局聚合效果差的问题。
method: 提出FSDT框架，将Transformer模型拆分为边缘端的特定组件和云端的共享全局层，结合离线强化学习进行资源分配。
result: "与基准方案相比，FSDT在异构环境中将QoE提升了10%，并将约98%的模型参数卸载到云端。"
conclusion: FSDT通过模型拆分和联邦学习的结合，有效解决了元宇宙边缘资源分配中的计算压力与性能优化平衡问题。
---

## 摘要
基于移动边缘计算（MEC）的无线元宇宙服务为用户提供了无束缚的沉浸式体验，在严格的延迟约束和视觉质量需求下，需要实现卓越的用户体验质量（QoE）。为实现这一目标，针对虚拟现实用户的基于 MEC 的智能资源分配需要通过 MEC 服务器之间的协调来支持，以利用分布式数据。联邦学习（FL）是一种极具前景的解决方案，可以与强化学习（RL）相结合，在 MEC 服务器之间开发通用策略。然而，传统的 FL 需要在 MEC 服务器和云端之间传输完整的模型参数，并且由于简单的全局聚合，尤其是在异构多无线电接入技术环境中，会面临性能下降的问题。为了应对这些挑战，本文提出了联邦拆分决策 Transformer（FSDT），这是一种离线强化学习框架，其中 Transformer 模型在 MEC 服务器和云端之间进行划分。特定于智能体的组件（例如基于 MEC 的嵌入层和预测层）实现了本地适应性，而云端的共享全局层则促进了跨 MEC 服务器的协作训练。实验结果表明，与基准方案相比，FSDT 在异构环境中将 QoE 提升了高达 10%，同时将近 98% 的 Transformer 模型参数卸载到云端，从而减轻了 MEC 服务器的计算负担。

## Abstract
Mobile edge computing (MEC) based wireless metaverse services offer an untethered, immersive experience to users, where the superior quality of experience (QoE) needs to be achieved under stringent latency constraints and visual quality demands. To achieve this, MEC-based intelligent resource allocation for virtual reality users needs to be supported by coordination across MEC servers to harness distributed data. Federated learning (FL) is a promising solution, and can be combined with reinforcement learning (RL) to develop generalized policies across MEC-servers. However, conventional FL incurs transmitting the full model parameters across the MEC-servers and the cloud, and suffer performance degradation due to naive global aggregation, especially in heterogeneous multi-radio access technology environments. To address these challenges, this paper proposes Federated Split Decision Transformer (FSDT), an offline RL framework where the transformer model is partitioned between MEC servers and the cloud. Agent-specific components (e.g., MEC-based embedding and prediction layers) enable local adaptability, while shared global layers in the cloud facilitate cooperative training across MEC servers. Experimental results demonstrate that FSDT enhances QoE for up to 10% in heterogeneous environments compared to baselines, while offloadingnearly 98% of the transformer model parameters to the cloud, thereby reducing the computational burden on MEC servers.

---

## 论文详细总结（自动生成）

## 论文总结：基于联邦拆分决策 Transformer 的元宇宙资源分配边缘学习

### 1. 核心问题与研究动机
*   **研究背景**：元宇宙服务（如无线 VR）对延迟和视觉质量（QoE）有极高要求。移动边缘计算（MEC）通过卸载渲染任务来降低延迟，但异构网络环境下的资源分配极具挑战。
*   **核心问题**：
    *   **在线强化学习（RL）缺陷**：如 DDPG 等算法在探索不足时难以收敛，且维护数字孪生（Digital Twin）开销巨大。
    *   **传统联邦学习（FL）局限**：在异构多无线电接入技术（multi-RAT）中，全量参数传输开销大，且简单的全局聚合（Naive Aggregation）会导致模型性能在个性化场景下下降。
*   **研究动机**：结合离线强化学习（决策 Transformer）、联邦学习和拆分学习（Split Learning），在保护隐私和降低边缘负担的同时，提升异构环境下的资源分配效率。

### 2. 方法论
论文提出了 **FSDT (Federated Split Decision Transformer)** 框架，核心思想如下：
*   **模型拆分（Split Architecture）**：
    *   **边缘端（MEC 服务器）**：部署轻量级的**嵌入层（Embedding）**和**预测层（Prediction）**，用于捕捉特定域（如 Wi-Fi 或 Sub-6GHz）的特征。
    *   **云端（Cloud）**：部署计算密集型的 **Transformer 解码器（Decoder）**，作为全局共享的知识库。
*   **离线强化学习建模**：将资源分配建模为序列建模问题。状态空间包括数据速率、各类延迟和 QoE；动作空间包括 CPU/GPU 频率分配、带宽分配及分辨率比例。
*   **两阶段训练流程（Algorithm 1）**：
    *   **阶段 1（本地自适应）**：固定云端解码器，各 MEC 服务器更新本地嵌入和预测层，并在同类智能体（Domain）间进行联邦聚合（FedAvg）。
    *   **阶段 2（全局协作）**：固定边缘层，云端利用来自所有 MEC 的嵌入数据训练共享的 Transformer 解码器。
*   **QoE 模型**：综合考虑了延迟惩罚、基于瓦片（Tile）的视频质量以及分辨率切换的平滑度。

### 3. 实验设计
*   **实验场景**：构建了一个异构多 RAT 系统，包含 5 种环境：UMB/UMi、UMB/InH、Sub-6GHz/UMi、Sub-6GHz/InH 和 Wi-Fi/InH。
*   **数据集**：使用公开的 VR 眼动追踪数据集（包含 200+ YouTube 视频），通过眼动坐标确定 FoV（视场角）内的瓦片质量。
*   **基准方法（Benchmarks）**：
    *   **C-DDPG / F-DDPG**：集中式和联邦式在线强化学习。
    *   **C-DT**：集中式离线决策 Transformer。
    *   **F-DT**：传统联邦离线决策 Transformer（全量参数聚合）。
    *   **FSDT**：本文提出的拆分架构方案。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **模型规模**：论文提供了详细的参数分布：
    *   云端解码器：约 4.74M 参数（占比 **98.79%**）。
    *   边缘端层：仅约 58K 参数（占比约 1.2%）。
    *   这种分布表明该方法显著降低了边缘端的存储和计算压力。

### 5. 实验数量与充分性
*   **实验规模**：在 5 种不同的异构 MEC 环境下进行测试，每个环境服务 4 个用户。
*   **充分性**：
    *   测试过程跨越了 100 个 Episode，并使用了 3 个不同的随机种子取平均值，保证了结果的统计意义。
    *   对比了在线与离线、集中式与分布式等多种架构，实验覆盖面较广。
    *   **客观性**：通过箱线图展示了 QoE 和延迟的分布，不仅关注平均值，还展示了算法的稳定性（方差）。

### 6. 主要结论与发现
*   **性能提升**：FSDT 在异构环境下的 QoE 比基准方案提升了约 **10%**，且延迟更低、更稳定。
*   **计算卸载**：成功将 **98%** 的模型参数卸载到云端，MEC 服务器仅需处理极小规模的参数更新。
*   **架构优势**：实验证明，传统的联邦学习（F-DT）在异构环境下表现甚至不如集中式（C-DT），而 FSDT 通过拆分架构保留了本地自适应性，克服了这一问题。
*   **稳定性**：离线 RL 架构（DT 系列）在处理元宇宙复杂轨迹时，比在线 RL（DDPG 系列）表现出更小的性能波动。

### 7. 亮点
*   **高效性**：巧妙结合了拆分学习和联邦学习，解决了“模型太大边缘跑不动”和“环境太杂模型不收敛”的矛盾。
*   **隐私与带宽平衡**：仅交换潜在表示（Latent Representations）而非原始数据，且相比全量参数交换，降低了通信开销。
*   **实际应用导向**：针对 VR 渲染的层次化视觉特性（FoV）进行了精细化建模。

### 8. 不足与局限
*   **数据依赖性**：作为离线 RL 框架，其性能高度依赖于专家轨迹（本研究中由 DDPG 生成）的质量。
*   **通信频率**：虽然单次传输量小，但拆分学习在训练过程中需要在边缘和云端之间频繁交换特征和梯度，这在极高动态的网络环境下可能产生新的瓶颈。
*   **算力细节缺失**：缺乏具体的硬件运行时间对比，难以直观评估训练阶段的实际能耗收益。

（完）
