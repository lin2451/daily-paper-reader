---
title: Energy Efficient Federated Learning with Hyperdimensional Computing over Wireless Communication Networks
title_zh: 无线通信网络中基于超维计算的高能效联邦学习
authors: "Yahao Ding, Yinchao Yang, Jiaxiang Wang, Zhaohui Yang, Dusit Niyato, Zhu Han, Mohammad Shikh-Bahaei"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21949v1"
tags: ["query:fl-ra"]
score: 9.0
evidence: 优化计算与通信的总能量消耗
tldr: 本文针对无线边缘网络中联邦学习的高能耗与隐私挑战，提出了一种结合超维计算（HDC）与差分隐私（DP）的FL-HDC-DP框架。该框架利用HDC的简单向量运算替代复杂的神经网络更新，并通过联合优化HDC维度、传输功率、带宽及CPU频率，在满足时延和隐私约束下最小化总能耗。研究贡献了描述HDC维度与收敛性关系的数学模型及高效的交替优化算法，为资源受限环境下的边缘智能提供了高效方案。
selection_source: fresh_fetch
motivation: 旨在解决传统联邦学习在资源受限的边缘网络中计算成本高昂且存在隐私泄露风险的问题。
method: 提出FL-HDC-DP框架，利用超维计算简化本地训练并结合差分隐私，通过交替优化算法实现计算与通信资源的联合分配。
result: "实验显示该框架比基准方案降低了83.3%的总能耗，且在达到目标准确率时，通信轮数仅为神经网络方案的约1/3.5。"
conclusion: 结合超维计算的联邦学习框架能显著提升无线边缘网络的能效与收敛速度，是实现绿色安全人工智能的有效途径。
---

## 摘要
本文研究了无线边缘网络中安全联邦学习（FL）的总能耗最小化问题。针对资源受限用户在传统神经网络（NN）联邦学习中面临的高计算成本和隐私挑战，我们提出了一种结合超维计算与差分隐私的新型联邦学习（FL-HDC-DP）框架。在该模型中，每个边缘用户采用超维计算（HDC）进行本地训练，利用简单的超向量运算替代复杂的神经网络更新，并应用差分隐私（DP）噪声来保护传输的模型信息。我们在延迟和隐私约束下优化计算与通信的总能耗。我们将该问题建模为一个优化问题，通过联合分配 HDC 维度、传输时间、系统带宽、发射功率和 CPU 频率，使所有用户的总能耗最小化。为了解决该问题，本文提出了一种 Sigmoid 变体函数，用于刻画 HDC 维度与达到目标精度所需的收敛轮数之间的关系。基于该模型，我们开发了两种交替优化算法，并在每次迭代中推导出了时间、频率、带宽和功率分配的闭式表达式。由于迭代算法需要可行的初始化，我们构建了一个可行性问题，并通过解决每轮传输时间最小化问题来获得可行的初始资源参数。仿真结果表明，与基准方案相比，所提出的 FL-HDC-DP 框架可减少高达 83.3% 的总能耗，同时在比神经网络基准方案少约 3.5 倍的通信轮数下达到约 90% 的准确率。

## Abstract
In this paper, we investigate a problem of minimizing total energy consumption for secure federated learning (FL) over wireless edge networks. To address the high computational cost and privacy challenges in conventional FL with neural networks (NN) for resource-constrained users, we propose a novel FL with hyperdimensional computing and differential privacy (FL-HDC-DP) framework. In the considered model, each edge user employs hyperdimensional computing (HDC) for local training, which replaces complex neural updates with simple hypervector operations, and applies differential privacy (DP) noise to protect transmitted model information. We optimize the total energy of computation and communication under both latency and privacy constraints. We formulate the problem as an optimization that minimizes the total energy of all users by jointly allocating HDC dimension, transmission time, system bandwidth, transmit power, and CPU frequency. To solve this problem, a sigmoid-variant function is proposed to characterize the relationship between the HDC dimension and the convergence rounds required to reach a target accuracy. Based on this model, we develop two alternating optimization algorithms, where closed-form expressions for time, frequency, bandwidth, and power allocations are derived at each iteration. Since the iterative algorithm requires a feasible initialization, we construct a feasibility problem and obtain feasible initial resource parameters by solving a per round transmission time minimization problem. Simulation results demonstrate that the proposed FL-HDC-DP framework achieves up to 83.3% total energy reduction compared with the baseline, while attaining about 90% accuracy in approximately 3.5X fewer communication rounds than the NN baseline.

---

## 论文详细总结（自动生成）

### 论文结构化总结：无线通信网络中基于超维计算的高能效联邦学习

#### 1. 核心问题与整体含义（研究动机和背景）
本文旨在解决**无线边缘网络中联邦学习（FL）的能效与隐私双重挑战**。
*   **研究动机**：传统的联邦学习通常依赖深度神经网络（NN），其训练过程涉及复杂的反向传播和浮点运算，对于计算资源和电池容量受限的边缘设备（如IoT传感器、可穿戴设备）而言负担过重。此外，模型更新在无线传输过程中面临隐私泄露风险。
*   **核心问题**：如何在保证用户隐私的前提下，通过联合优化计算模式和通信资源，最小化联邦学习过程中的总能量消耗。

#### 2. 方法论：核心思想与技术细节
论文提出了 **FL-HDC-DP** 框架，其核心思想是将**超维计算（HDC）**引入联邦学习，并结合**差分隐私（DP）**。
*   **超维计算（HDC）**：受大脑启发，将数据映射到高维空间（通常为 $10^3$ 至 $10^4$ 维）。它使用简单的向量运算（叠加、绑定、置换）代替 NN 的复杂权重更新，具有极高的计算效率和硬件友好性。
*   **差分隐私（DP）**：采用零均值集中差分隐私（zCDP）机制，在本地模型上传前添加高斯噪声，提供严格的数学隐私保证。
*   **收敛性建模**：创新性地提出了一个 **Sigmoid 变体函数**，用于刻画 HDC 维度 $d$ 与达到目标精度所需的通信轮数 $J_d$ 之间的非线性关系。
*   **联合优化算法**：构建了一个最小化总能耗的优化问题，变量包括 HDC 维度、传输功率、带宽分配、传输时间和 CPU 频率。采用**两层交替优化（Alternating Optimization）算法**：
    *   **外层**：优化 HDC 维度 $d$。
    *   **内层**：在给定维度下，通过 KKT 条件和拉格朗日乘子法推导闭式解，优化通信与计算资源分配。
*   **可行性初始化**：通过解决一个每轮传输时间最小化问题，确保迭代算法从可行解开始。

#### 3. 实验设计
*   **数据集**：使用经典的 **MNIST** 手写数字识别数据集。
*   **场景设置**：考虑了 **IID（独立同分布）** 和 **Non-IID（非独立同分布）** 两种数据分布。
*   **基准（Benchmark）**：
    *   **FL-NN-DP**：传统的基于神经网络的联邦学习（带差分隐私）。
    *   **固定参数方案**：如固定 HDC 维度、固定发射功率或固定带宽的对比方案。
*   **对比指标**：模型准确率、收敛轮数、总能量消耗（焦耳）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号或训练时长。
*   **隐含信息**：由于 HDC 的核心优势在于低功耗和简单运算，实验可能是在标准 CPU 环境下进行的仿真。论文详细列出了 CPU 周期参数（如编码、聚合、相似度计算所需的周期数），用于计算能耗模型。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了不同 HDC 维度（3000 到 10000）对准确率的影响。
    *   对比了不同隐私预算 $\epsilon$（10, 20, 25）下的表现。
    *   进行了 Non-IID 场景下的鲁棒性测试。
    *   针对带宽、发射功率等资源参数进行了多组消融实验。
*   **充分性评价**：实验设计较为全面，涵盖了从算法性能（准确率/收敛性）到系统性能（能耗/资源分配）的多个维度。通过拟合曲线验证了 Sigmoid 变体模型的有效性，实验结果具有较强的说服力。

#### 6. 主要结论与发现
*   **高能效**：提出的优化框架相比于固定参数的基准方案，最高可降低 **83.3%** 的总能耗。
*   **快速收敛**：FL-HDC-DP 在达到 90% 准确率时，所需的通信轮数比 NN 基准方案少约 **3.5 倍**。
*   **维度权衡**：HDC 维度 $d$ 是关键变量。增加维度能减少收敛轮数，但会增加单轮的计算和通信开销。实验发现存在一个最优维度（如本实验中的 $d=4000$）使总能耗最低。
*   **距离敏感性**：距离基站较远的用户需要分配更多的带宽和更高的 CPU 频率，以补偿较差的信道条件并满足时延约束。

#### 7. 优点（亮点）
*   **范式创新**：首次将 HDC 维度作为优化变量引入联邦学习的资源分配问题中。
*   **数学严谨性**：为复杂的维度-收敛关系建立了实用的数学模型，并推导了资源分配的闭式解，降低了算法复杂度。
*   **边缘友好**：证明了 HDC 在资源受限设备上替代 NN 的巨大潜力，特别是在对实时性和功耗要求极高的场景。

#### 8. 不足与局限
*   **任务复杂度限制**：实验仅限于 MNIST 这种相对简单的分类任务。HDC 在处理超大规模图像（如 ImageNet）或复杂自然语言处理任务时的表现尚未验证。
*   **隐私开销**：虽然引入了 DP，但噪声对 HDC 准确率的负面影响在 Non-IID 场景下依然显著，且 HDC 的高维特性可能导致噪声累积。
*   **硬件假设**：能耗模型基于特定的 CPU 周期假设，实际硬件（如专门的 HDC 处理器）上的表现可能存在偏差。

（完）
