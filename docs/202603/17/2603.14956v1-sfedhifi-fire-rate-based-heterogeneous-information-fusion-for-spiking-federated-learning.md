---
title: "SFedHIFI: Fire Rate-Based Heterogeneous Information Fusion for Spiking Federated Learning"
title_zh: SFedHIFI：基于脉冲率的脉冲联邦学习异构信息融合
authors: "Ran Tao, Qiugang Zhan, Shantian Yang, Xiurui Xie, Qi Tian, Guisong Liu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14956v1"
tags: ["query:fl-ra"]
score: 8.0
evidence: 针对异构客户端根据本地资源进行自适应模型部署
tldr: 针对脉冲联邦学习（SFL）中客户端资源异构且要求模型同质化的问题，本文提出了SFedHIFI框架。该框架利用通道级矩阵分解，允许客户端根据本地资源自适应部署不同规模的SNN模型，并通过基于脉冲发放率的异构信息融合模块实现跨尺度模型聚合。实验证明，该方法在保持高能效的同时，显著提升了异构场景下的模型性能，为资源受限环境下的协作学习提供了新思路。
selection_source: fresh_fetch
motivation: 现有的脉冲联邦学习方法通常要求模型结构一致，难以兼容现实中计算资源受限且具有异构性的客户端。
method: 通过通道级矩阵分解实现自适应复杂度的模型部署，并利用异构信息融合模块进行跨尺度的模型参数聚合。
result: 在多个基准测试中，SFedHIFI性能均优于现有基准，且在仅损失微小精度的情况下比传统ANN联邦学习大幅降低了能耗。
conclusion: SFedHIFI成功实现了异构脉冲联邦学习，有效提升了不同规模模型间的知识利用率与系统整体的能效比。
---

## 摘要
脉冲联邦学习（SFL）因脉冲神经网络（SNN）的高能效性而得到了广泛研究。然而，现有的 SFL 方法要求模型同质性，并假设所有客户端都拥有充足的计算资源，导致一些资源受限的客户端被排除在外。为了应对现实场景中普遍存在的系统异构性，构建允许客户端根据其本地资源自适应部署不同规模模型的异构 SFL 系统至关重要。为此，我们提出了 SFedHIFI，这是一种具有基于脉冲率的异构信息融合的新型脉冲联邦学习框架。具体而言，SFedHIFI 采用通道级矩阵分解，在具有异构资源的客户端上部署复杂度自适应的 SNN 模型。在此基础上，所提出的异构信息融合模块实现了不同宽度模型之间的跨尺度聚合，从而提高了对多样化本地知识的利用率。在三个公共基准数据集上的广泛实验表明，SFedHIFI 可以有效地实现异构 SFL，其表现始终优于所有三种基准方法。与基于人工神经网络（ANN）的联邦学习相比，它在仅损失极小精度的情况下实现了显著的节能。

## Abstract
Spiking Federated Learning (SFL) has been widely studied with the energy efficiency of Spiking Neural Networks (SNNs). However, existing SFL methods require model homogeneity and assume all clients have sufficient computational resources, resulting in the exclusion of some resource-constrained clients. To address the prevalent system heterogeneity in real-world scenarios, enabling heterogeneous SFL systems that allow clients to adaptively deploy models of different scales based on their local resources is crucial. To this end, we introduce SFedHIFI, a novel Spiking Federated Learning framework with Fire Rate-Based Heterogeneous Information Fusion. Specifically, SFedHIFI employs channel-wise matrix decomposition to deploy SNN models of adaptive complexity on clients with heterogeneous resources. Building on this, the proposed heterogeneous information fusion module enables cross-scale aggregation among models of different widths, thereby enhancing the utilization of diverse local knowledge. Extensive experiments on three public benchmarks demonstrate that SFedHIFI can effectively enable heterogeneous SFL, consistently outperforming all three baseline methods. Compared with ANN-based FL, it achieves significant energy savings with only a marginal trade-off in accuracy.

---

## 论文详细总结（自动生成）

这篇论文提出了 **SFedHIFI**，一个针对脉冲神经网络（SNN）设计的异构联邦学习框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的脉冲联邦学习（SFL）通常假设所有客户端使用相同的模型架构（同质性），但在现实的边缘计算场景中，设备硬件资源（计算、内存）高度异构。
*   **研究动机**：
    *   **系统异构性**：资源受限的设备无法运行大型模型，若直接排除这些设备会损失宝贵的本地数据知识。
    *   **SNN 的特殊性**：简单的模型压缩（如通道剪枝）在 SNN 中会导致严重的特征稀疏和信息丢失，使得传统的异构联邦学习方法在 SNN 上表现不佳。
    *   **能效需求**：利用 SNN 的事件驱动特性，在异构分布式环境中实现低功耗的协作学习。

### 2. 核心方法论
SFedHIFI 框架主要包含以下关键技术：
*   **通道级矩阵分解（Channel-wise Matrix Decomposition）**：
    *   将卷积层权重 $W$ 分解为一个**统一共享基矩阵 $B$**（代表全局通道空间）和一个**客户端特定规模因子矩阵 $M$**（编码异构通道维度）。
    *   通过调整 $M$ 的维度，客户端可以根据资源部署不同宽度（如 0.25x, 0.5x, 0.75x, 1.0x）的 SNN 模型。
*   **基于脉冲率的异构信息融合（HIFI 模块）**：
    *   **层识别**：利用 SNN 的生物特性——脉冲发放率（Fire Rate）来衡量各层的信息密度，识别出信息最丰富的层。
    *   **信息解耦与融合**：对识别出的高信息密度层，利用 **Tucker 分解** 将不同尺度的 $M$ 矩阵解耦为核心张量和因子矩阵，在服务器端聚合核心张量，实现跨尺度的知识共享。
*   **局部优化目标**：采用时间高效训练（TET）损失，并引入正交正则化项来约束基矩阵 $B$，以增强其全局泛化能力。

### 3. 实验设计
*   **数据集**：Fashion-MNIST、CIFAR-10、CIFAR-100。
*   **数据分布**：涵盖 IID（独立同分布）和 Non-IID（非独立同分布，使用 Dirichlet 分布 $\alpha=0.5, 0.3$）。
*   **模型规模**：设置了四个宽度尺度 $p = \{0.25, 0.5, 0.75, 1.0\}$。
*   **对比基准（Benchmarks）**：
    *   **FedAVG、FedProx、FedNova**：这些方法在异构场景下只能在相同规模的模型间进行聚合。
    *   **消融实验**：对比了无 HIFI 模块、固定层融合、随机层融合等变体。
    *   **ANN 对比**：与基于人工神经网络（ANN）的异构联邦学习在精度和能耗上进行对比。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件型号（如 GPU 类型）、数量或总训练时长。
*   **实现细节**：提到使用了 `Spikejelly` 框架，SNN 采用 LIF 神经元，时间步 $T=10$，客户端总数为 10 个，每轮随机选择 5 个参与。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个数据集、3 种数据分布、4 种模型尺度下进行了详尽测试。
*   **充分性评价**：实验设计较为全面，不仅验证了准确率的提升，还通过消融实验证明了 HIFI 模块中“脉冲率引导”的有效性。此外，还专门进行了能耗分析，符合 SNN 研究的初衷。实验对比了多种经典的联邦学习改进算法，具有较好的客观性和公平性。

### 6. 主要结论与发现
*   **性能提升**：SFedHIFI 在所有数据集和分布下均优于基准方法。在 CIFAR-100 的 Non-IID 场景下，准确率提升尤为显著（最高达 19% 以上）。
*   **跨尺度增益**：通过 HIFI 模块，小规模模型能从大规模模型中获取知识，显著缓解了剪枝带来的性能下降。
*   **能效优势**：与同等规模的 ANN 联邦学习相比，SFedHIFI 在仅损失不到 1% 精度的情况下，实现了约 **43.74% 的能耗降低**。

### 7. 优点与亮点
*   **创新性**：首次将脉冲发放率这一 SNN 固有属性引入异构联邦学习的信息融合过程，解决了 SNN 压缩后的信息稀疏问题。
*   **灵活性**：通过矩阵分解实现了模型宽度的动态调整，能够适应极度受限的边缘设备。
*   **实用性**：在提升性能的同时兼顾了 SNN 的低功耗特性，为真实世界的神经形态硬件部署提供了理论支持。

### 8. 不足与局限
*   **客户端规模较小**：实验仅模拟了 10 个客户端，在大规模分布式系统（如成千上万个节点）中的收敛性和通信开销尚待验证。
*   **计算开销**：虽然推理能耗低，但在服务器端进行 Tucker 分解和矩阵重构会增加额外的计算负担，论文未详细讨论这一部分的开销。
*   **任务复杂度**：实验主要集中在图像分类任务，对于更复杂的时空序列任务或回归任务的适用性有待探索。

（完）
