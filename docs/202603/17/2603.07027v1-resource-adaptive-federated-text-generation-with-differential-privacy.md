---
title: Resource-Adaptive Federated Text Generation with Differential Privacy
title_zh: 具有差分隐私的资源自适应联邦文本生成
authors: "Jiayi Wang, John Gounley, Heidi Hanson"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07027v1"
tags: ["query:fl-ra"]
score: 8.0
evidence: 适应客户端能力的灵活参与框架
tldr: 针对跨机构联邦学习中计算资源异构和隐私保护的挑战，本文提出一种资源自适应的差分隐私（DP）文本生成框架。该框架允许强算力客户端进行DP微调，而弱算力客户端通过轻量级DP投票机制贡献数据特征。通过引入控制代码确保合成数据与全局分布一致，在保护隐私的同时解决了数据倾斜问题，显著提升了合成文本的质量和下游任务的鲁棒性。
selection_source: carryover_cache
motivation: 传统的联邦微调往往因客户端计算资源不均而排除弱算力节点，导致数据偏斜并放大差分隐私噪声对模型性能的负面影响。
method: 提出一种双阶段框架，结合强客户端的DP联邦微调与弱客户端的轻量级DP投票机制，并利用控制代码引导语义一致的合成文本生成。
result: 实验证明，该框架在差分隐私和资源异构环境下显著改善了合成数据的分布对齐度，并增强了下游任务的性能。
conclusion: 该研究通过灵活的参与机制有效整合了异构客户端资源，为隐私保护下的联邦文本生成提供了一种高效且鲁棒的解决方案。
---

## 摘要
在跨机构联邦学习（FL）中，由于隐私法规的限制，敏感文本数据集仍局限于本地组织，这使得针对每个下游任务的重复训练既耗费通信资源又对隐私保护有极高要求。一个极具前景的替代方案是生成近似全局分布且可跨任务重用的差分隐私（DP）合成数据集。然而，预训练大语言模型（LLMs）在领域偏移下往往表现不佳，且联邦微调受到计算异构性的阻碍：只有资源丰富的客户端才能更新模型，而性能较弱的客户端则被排除在外，这放大了数据偏斜和差分隐私噪声的负面影响。我们提出了一种能够适应客户端能力的灵活参与框架。强客户端执行差分隐私联邦微调，而弱客户端则通过一种轻量级的差分隐私投票机制来优化合成文本。为了确保合成数据能够反映全局数据集，我们应用了代表每个客户端数据比例的控制代码（如标签、主题、元数据），并将投票限制在语义一致的子集中。这种两阶段方法对弱客户端仅需一轮通信，并整合了所有参与者的贡献。实验表明，我们的框架在差分隐私和异构性条件下提高了分布对齐度和下游鲁棒性。

## Abstract
In cross-silo federated learning (FL), sensitive text datasets remain confined to local organizations due to privacy regulations, making repeated training for each downstream task both communication-intensive and privacy-demanding. A promising alternative is to generate differentially private (DP) synthetic datasets that approximate the global distribution and can be reused across tasks. However, pretrained large language models (LLMs) often fail under domain shift, and federated finetuning is hindered by computational heterogeneity: only resource-rich clients can update the model, while weaker clients are excluded, amplifying data skew and the adverse effects of DP noise. We propose a flexible participation framework that adapts to client capacities. Strong clients perform DP federated finetuning, while weak clients contribute through a lightweight DP voting mechanism that refines synthetic text. To ensure the synthetic data mirrors the global dataset, we apply control codes (e.g., labels, topics, metadata) that represent each client's data proportions and constrain voting to semantically coherent subsets. This two-phase approach requires only a single round of communication for weak clients and integrates contributions from all participants. Experiments show that our framework improves distribution alignment and downstream robustness under DP and heterogeneity.

---

## 论文详细总结（自动生成）

这篇论文题为《具有差分隐私的资源自适应联邦文本生成》（Resource-Adaptive Federated Text Generation with Differential Privacy），由橡树岭国家实验室的研究团队撰写。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在跨机构（Cross-silo）联邦学习中，如何利用分布在不同机构、受隐私保护且计算资源极度不均的文本数据，生成高质量的差分隐私（DP）合成数据集。
*   **研究动机**：
    *   **计算异构性**：大语言模型（LLM）微调需要极高算力，导致只有少数“强客户端”能参与，而“弱客户端”被排除，造成严重的数据偏斜。
    *   **领域偏移**：预训练模型在特定领域（如医疗）表现不佳，必须微调。
    *   **隐私与噪声**：差分隐私随机梯度下降（DP-SGD）引入的噪声会降低生成文本质量，且参与度不足会放大这种负面影响。

### 2. 方法论
论文提出了一种**双阶段资源自适应框架**：
*   **第一阶段：DP 联邦微调（针对强客户端）**：
    *   具备算力的客户端使用 DP-SGD 算法对全局生成模型（如 GPT-2）进行微调。
    *   引入**控制代码（Control Codes）**（如标签、主题）：模型学习条件概率 $p(x|c)$，使生成过程可控。
*   **第二阶段：DP 投票细化（针对弱客户端）**：
    *   **分析高斯机制**：所有客户端（包括弱客户端）上传受 DP 保护的本地控制代码分布（Profile）。
    *   **轻量级投票**：弱客户端无需反向传播，仅需对服务器生成的候选文本进行 K-最近邻（KNN）投票，判断合成文本是否符合本地真实分布。
    *   **重采样**：服务器根据聚合的 DP 投票结果对合成数据进行加权重采样，从而修正强客户端微调带来的偏差。

### 3. 实验设计
*   **数据集**：
    *   **Yelp Reviews**：用于商业分类和评分预测任务。
    *   **PubMed Abstracts**：用于医学领域适配和命名实体识别（NER）任务。
*   **场景设置**：模拟 IID（独立同分布）和 Non-IID（非独立同分布）环境，设置不同比例的强客户端（1% 到 40%）。
*   **基准方法（Benchmark）**：
    *   预训练模型直接生成（Zero-shot）。
    *   无隐私保护的联邦微调（$\epsilon = \infty$）。
    *   仅有 DP 微调但无投票细化的模型。
*   **对比指标**：下游任务的准确率/F1 分数、MAUVE 分数（分布对齐度）、NER 性能。

### 4. 资源与算力
*   **硬件环境**：使用 NVIDIA DGX 系统，配备 **8 张 H100 GPU (80GB)**，2 颗 AMD EPYC 9654 CPU 和 2TB 系统内存。
*   **模型规模**：实验涵盖了 GPT-2、GPT-2-large 以及 **LLaMA-7B**（结合 LoRA 技术）。
*   **训练细节**：微调轮数在 50 到 200 轮之间，使用了混合精度训练。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度实验，包括不同强客户端占比、不同隐私预算（$\epsilon=4, 8$）、IID 与多种 Non-IID 划分方式。
*   **消融实验**：针对采样率 $r$、投票数 $K$、生成温度等超参数进行了详细的消融研究。
*   **充分性评价**：实验设计较为全面，不仅验证了小模型，还扩展到了 LLaMA-7B 等主流大模型，证明了该框架在不同资源配置下的普适性和公平性。

### 6. 主要结论与发现
*   **部分参与亦有效**：即使只有 1% 的强客户端参与微调，效果也优于纯预训练模型。
*   **细化阶段至关重要**：投票细化机制能显著抵消 DP 噪声和数据异构带来的负面影响，在某些 Non-IID 场景下，带细化的 DP 模型性能甚至接近非隐私模型。
*   **资源包容性**：该框架成功让无法进行模型训练的弱客户端通过低功耗的“投票”贡献了价值，提升了全局合成数据的鲁棒性。

### 7. 优点
*   **架构灵活**：巧妙地将计算密集型的微调与计算轻量级的投票结合，解决了联邦学习中的“木桶效应”。
*   **隐私保护严密**：在微调、分布统计和投票三个环节均应用了严格的 DP 保证。
*   **实用性强**：生成的合成数据集可多次用于不同下游任务，减少了重复联邦训练的通信开销。

### 8. 不足与局限
*   **控制代码依赖**：假设控制代码（如标签）是预定义的且非私有的，在某些完全无监督的场景下可能难以应用。
*   **编码器质量**：投票机制依赖于句子嵌入（Sentence Embedding）的质量，若预训练编码器在极特定领域表现差，投票准确性可能受限。
*   **通信开销**：虽然弱客户端只参与一轮，但服务器需要向其广播大量候选合成文本以供投票，这在带宽受限的环境下仍有优化空间。

（完）
