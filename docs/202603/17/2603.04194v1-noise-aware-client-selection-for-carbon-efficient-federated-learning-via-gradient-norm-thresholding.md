---
title: Noise-aware Client Selection for carbon-efficient Federated Learning via Gradient Norm Thresholding
title_zh: 通过梯度范数阈值化实现碳高效联邦学习的噪声感知客户端选择
authors: "Patrick Wilhelm, Inese Yilmaz, Odej Kao"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04194v1"
tags: ["query:fl-ra"]
score: 8.0
evidence: 碳效率联邦学习中的噪声感知客户端选择
tldr: 本研究针对碳效率联邦学习中数据质量不可知的问题，提出了一种噪声感知的客户端选择方法。在利用可再生能源降低AI训练碳足迹的基础上，针对现有策略易受噪声数据干扰的缺陷，引入了基于梯度范数阈值的过滤机制。该方法通过探测轮次识别并排除低质量数据，在保证环境可持续性的同时提升了模型的鲁棒性与收敛性能，为实际部署低碳且高效的联邦学习提供了新方案。
selection_source: carryover_cache
motivation: 现有的碳效率联邦学习策略在追求低碳排放时，往往会误选含有噪声数据的客户端，导致模型性能下降。
method: 提出一种基于梯度范数阈值和探测轮次的模块化过滤机制，用于在客户端选择过程中识别并剔除噪声数据。
result: 实验证明该方法能有效提升模型在噪声环境下的鲁棒性，并在严格的碳预算限制下实现更优的训练效果。
conclusion: 将噪声过滤与碳效率策略相结合，是实现高性能、可持续联邦学习的关键路径。
---

## 摘要
训练大规模神经网络需要大量的计算能力和能源。联邦学习支持在地理分布的数据中心之间进行分布式模型训练，利用可再生能源来减少人工智能训练的碳足迹。已经开发了各种客户端选择策略，以使可再生能源的波动性与联邦系统中稳定且公平的模型训练相协调。然而，由于联邦学习的隐私保护特性，客户端设备上的数据质量仍然未知，这给有效的模型训练带来了挑战。在本文中，我们在最先进的碳高效联邦学习客户端选择策略之上引入了一种模块化方法。我们的方法通过结合噪声客户端数据过滤来增强鲁棒性，在数据质量未知的情况下提高了模型性能和可持续性。此外，我们探讨了碳预算对模型收敛的影响，平衡了效率和可持续性。通过广泛的评估，我们证明了基于本地客户端损失的现代客户端选择策略倾向于选择具有噪声数据的客户端，最终降低了模型性能。为了解决这个问题，我们提出了一种利用探测轮次的梯度范数阈值化机制，以实现更有效的客户端选择和噪声检测，为碳高效联邦学习的实际部署做出了贡献。

## Abstract
Training large-scale Neural Networks requires substantial computational power and energy. Federated Learning enables distributed model training across geospatially distributed data centers, leveraging renewable energy sources to reduce the carbon footprint of AI training. Various client selection strategies have been developed to align the volatility of renewable energy with stable and fair model training in a federated system. However, due to the privacy-preserving nature of Federated Learning, the quality of data on client devices remains unknown, posing challenges for effective model training. In this paper, we introduce a modular approach on top to state-of-the-art client selection strategies for carbon-efficient Federated Learning. Our method enhances robustness by incorporating a noisy client data filtering, improving both model performance and sustainability in scenarios with unknown data quality. Additionally, we explore the impact of carbon budgets on model convergence, balancing efficiency and sustainability. Through extensive evaluations, we demonstrate that modern client selection strategies based on local client loss tend to select clients with noisy data, ultimately degrading model performance. To address this, we propose a gradient norm thresholding mechanism using probing rounds for more effective client selection and noise detection, contributing to the practical deployment of carbon-efficient Federated Learning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Noise-aware Client Selection for carbon-efficient Federated Learning via Gradient Norm Thresholding》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着大模型训练带来的碳排放问题日益严重，**碳感知联邦学习（Carbon-aware FL）** 成为一种极具前景的解决方案。它通过在地理分布的数据中心之间调度任务，利用当地可再生能源（如风能、太阳能）的盈余来降低AI训练的碳足迹。
然而，现有的客户端选择策略（如 Oort）通常依赖**训练损失（Loss）**来衡量客户端的贡献度。在隐私保护的 FL 环境中，数据质量是不可知的。论文指出一个关键矛盾：**噪声或损坏的数据往往会产生极高的 Loss**。如果系统盲目选择高 Loss 的客户端，会误将噪声引入训练，导致模型性能大幅下降，甚至在追求低碳的同时牺牲了模型效用。

### 2. 论文提出的方法论
论文提出了一种模块化的噪声感知过滤机制，核心思想是利用**梯度范数（Gradient Norm）**作为数据质量的代理指标，而非单纯的 Loss。

*   **梯度范数探测（Probing Round）：** 在训练初期引入探测轮次，计算每个客户端 $i$ 的统计效用 $U(i)$。公式定义为：
    $$U(i) = |B_i| \cdot \sqrt{\frac{1}{|B_i|} \sum_{k \in B_i} \|\nabla f(k)\|^2}$$
    其中 $\nabla f(k)$ 是样本 $k$ 的梯度，$B_i$ 是本地数据集。梯度范数能更有效地反映损失函数的曲率，从而区分“有价值的硬样本”和“有害的噪声数据”。
*   **阈值过滤机制：** 服务器根据探测结果设定阈值，仅保留满足 $utility \ge c \cdot \max(utility)$ 的客户端（$c$ 为配置系数），剔除疑似含有噪声的客户端。
*   **碳预算分配（OortCA）：** 在客户端选择中引入硬性的碳预算约束。通过求解一个优化问题，在满足每轮碳排放限制 $B_t$ 的前提下，最大化入选客户端的总效用得分。

### 3. 实验设计
*   **数据集与模型：**
    *   CIFAR-10（使用简单 CNN）。
    *   CIFAR-100（使用 DenseNet-121）。
    *   Tiny ImageNet（使用 EfficientNet-B1）。
*   **场景设置：** 模拟 30 个位于美国不同能源区域的客户端，数据按 Dirichlet 分布（$\alpha=10$）呈现非 IID 特性。
*   **噪声模拟：** 随机选取 6 个客户端，对其图像添加均值为 0、标准差为 1 的高斯噪声。
*   **碳数据：** 使用来自 Electricity Maps 的真实历史每小时碳强度数据。
*   **Benchmark（对比方法）：**
    *   **Random：** 随机选择客户端。
    *   **Oort：** 基于 Loss 的最先进客户端选择策略。
    *   **RandomWT / OortWT：** 加入了论文提出的梯度范数阈值过滤（With Thresholding）的版本。
    *   **OortCA / OortCAWT：** 加入碳预算感知及噪声过滤的综合策略。

### 4. 资源与算力
*   **算力说明：** 论文**未明确说明**具体的硬件型号（如 GPU 种类、数量）或总训练时长。
*   **模拟参数：** 实验设定每个训练轮次对应 1 小时，共模拟 100 轮。假设每个客户端在训练期间消耗 1kWh 的电能。

### 5. 实验数量与充分性
*   **实验规模：** 论文在三个不同复杂度的模型和数据集上进行了验证，涵盖了从简单 CNN 到复杂的 EfficientNet。
*   **消融与对比：** 实验对比了不同碳预算水平（从 0% 到 100% 增量）对精度的影响，并针对噪声数据和干净数据两种场景分别进行了测试。
*   **客观性：** 实验使用了真实的碳强度数据和公认的 FL 基准算法（Oort），实验设计较为合理。但 30 个客户端的规模在联邦学习研究中属于中等偏小，对于更大规模（成千上万客户端）的扩展性讨论略显不足。

### 6. 论文的主要结论与发现
*   **Loss 陷阱：** 证实了基于 Loss 的策略（如 Oort）在噪声环境下会过度选择损坏的客户端，导致精度显著下降。
*   **过滤有效性：** 引入梯度范数阈值后，系统能准确识别并剔除噪声客户端，使模型收敛更稳定且最终精度更高。
*   **碳预算红利：** 即使在碳预算仅为基准排放量 40% 的情况下，通过智能选择高价值客户端，模型仍能达到与无约束训练相当的精度。
*   **效率提升：** 虽然探测轮次增加了初期开销，但由于模型收敛更快，达到目标精度所需的总碳排放量反而降低了。

### 7. 优点
*   **鲁棒性与可持续性的结合：** 首次深入探讨了碳感知 FL 中数据质量对环境效率的影响。
*   **模块化设计：** 提出的阈值过滤机制可以作为插件集成到现有的各种客户端选择算法中。
*   **理论支撑：** 借鉴了深度学习中的“关键学习期（Critical Learning Periods）”理论，利用梯度范数近似 Fisher 信息矩阵，具有一定的理论深度。

### 8. 不足与局限
*   **探测开销：** 初始探测轮次需要所有客户端参与计算梯度范数，这在带宽受限或客户端极多的情况下可能产生较大的通信和计算负担。
*   **超参数敏感性：** 阈值系数 $c$ 的选择对结果影响较大，论文提到在实际中需要依赖经验或代表性子集来确定，缺乏自适应调整机制。
*   **静态假设：** 实验假设客户端的电力需求是均等的，且数据质量在训练过程中保持不变，未考虑动态变化的噪声环境或异构的计算能力。

（完）
